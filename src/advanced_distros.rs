use crate::utils::{Distro, cut_space, collect_page, FormatUrl};
pub mod add_distro;
use add_distro::BasicDistros;


pub fn advanced_distros() -> Vec<Distro> {
    let mut distros: Vec<Distro> = Vec::new();


    distros
}

